# Optional: plain token for Dev Faucet mint only when vault has no underlyingToken yet.
# Do NOT set this to another chain's address — wrap/trade uses on-chain vault/router underlying only.
# VITE_UNDERLYING_TOKEN=0x...
